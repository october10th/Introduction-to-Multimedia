function F_out=noiseshape(F_in, c)
% noise shape & dither
F_in=128*F_in;% 8 bit
F_out=floor(F_in);
D=-1+2*rand(1);
F_dith=F_in+D;
e=0;
for i=1:1:length(F_in)
    F_in(i)=F_in(i)+D+c*e;
    F_out(i)=floor(F_in(i));
    e=F_in(i)-F_out(i);
end
end